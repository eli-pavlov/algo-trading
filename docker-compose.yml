version: '3.8'

services:
  algo-bot:
    image: algo-trader
    volumes:
      # PERSIST THE DB (Critical)
      - ./data:/app/data 
      # CACHE YFINANCE (Efficiency)
      - ./cache:/app/cache
      # LOGS
      - ./logs:/app/logs
    environment:
      - DB_PATH=/app/data/trading.db